import { datas } from "../../data/RessourcesData";

const initialState = {
  ressources: datas,
  selectedRessources: [],
};

// Reducer

const ReducerRessources = (state = initialState, action) => {
  return state;
};

export default ReducerRessources;
